<div class="row">
  <div class="col-md-12">
    <h1>Delete Price Book Page</h1>

    <%= form_for(@price_book_page, method: :delete) do |f| %>
      Are you sure?
      <button class="btn btn-default">Delete</button>
    <% end %>

    <%= link_to 'Back', book_pages_path, class: 'btn btn-default'%>
  </div>
</div>
