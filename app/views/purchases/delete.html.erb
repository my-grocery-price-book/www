<div class="row">
  <div class="col-md-12">
    <h1>Destroy Purchase and Items</h1>

    <%= form_for(@purchase, url: purchase_path(@purchase), method: :delete) do |f| %>
      Are you sure?
      <button class="btn btn-default">Destroy</button>
    <% end %>

    <%= link_to 'Edit', edit_purchase_path(@purchase), class: 'btn btn-default' %>
    <%= link_to 'List', purchases_path, class: 'btn btn-default' %>
  </div>
</div>
