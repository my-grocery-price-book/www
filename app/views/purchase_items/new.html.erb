<div class="row">
  <div class="col-md-12">
    <h1>New Purchase Item</h1>

    <%= form_for(@purchase_item, url: purchase_items_path(@purchase)) do |f| %>
      <%= render 'form', f: f %>
    <% end %>

    <%= link_to 'Back', purchase_items_path, class: 'btn btn-default' %>
  </div>
</div>
