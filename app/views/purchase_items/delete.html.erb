<div class="row">
  <div class="col-md-12">
    <h1>Delete Purchase Item</h1>

    <%= form_for(@purchase_item, url: purchase_item_path(@purchase, @purchase_item), method: :delete) do |f| %>
      Are you sure?
      <button class="btn btn-default">Delete</button>
    <% end %>

    <%= link_to 'Back', edit_purchase_path(@purchase), class: 'btn btn-default' %>
  </div>
</div>
