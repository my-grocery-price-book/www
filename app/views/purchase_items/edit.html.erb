<h1>Editing Purchase Item</h1>

<%= form_for(@purchase_item, url: purchase_item_path(@purchase,@purchase_item)) do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Show', purchase_item_path(@purchase,@purchase_item) %> |
<%= link_to 'Back', purchase_items_path(@purchase) %>
