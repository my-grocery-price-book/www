<%= render 'form', book: @book,
                   form_action: book_regions_path,
                   back_href: price_book_pages_path,
                   region_options: book_region_options(@regions) %>