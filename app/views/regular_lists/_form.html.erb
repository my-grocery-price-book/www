<%= form_for(@regular_list) do |f| %>
  <% if @regular_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regular_list.errors.count, "error") %> prohibited this regular_list from being saved:</h2>
      <ul>
      <% @regular_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="form-group">
    <%= f.label :category %><br>
  <%= f.select :category, ['Bakery','Fresh','Frozen','Ready Meals', 'Food Cupboard', 'Snacks and Sweets', 'Drinks', 'Household', 'Pets','Baby','Health and Beauty', 'Other'], include_blank: true %>
  </div>
  <%= f.submit 'Create Regular Item', class: 'btn btn-default' %>
<% end %>
