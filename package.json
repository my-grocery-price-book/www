{
  "name": "react-webpack-rails-my-grocery-book",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "5.10.0",
    "npm": "3.5.0"
  },
  "scripts": {
    "postinstall": "cd client && npm install",
    "test": "cd client && npm run test"
  }
}
